name: VCRS Audit

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  vcrs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Production build
        run: npm run build
      - name: VCRS static audit
        run: node scripts/vcrs.mjs
